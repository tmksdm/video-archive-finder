# Состояние проекта

## Текущий результат

Реализован смешанный поиск по папкам и именам видеофайлов с независимым режимом каждого источника; существующий portable release candidate `1.0.0-rc.2` не пересобирался.

## Подтверждённое состояние

- Все добавленные источники по умолчанию включены в область поиска; у каждого источника есть независимый флажок «Искать».
- При добавлении источника можно выбрать «только названия папок», «только имена видеофайлов» или оба типа; ранее сохранённые источники остаются в режиме поиска по папкам.
- Полный обход перечисляет видеофайлы только для соответствующих источников, но не запускает массовый FFprobe-анализ и создание миниатюр.
- Результаты по папкам и файлам из разных выбранных источников объединяются в одном дереве; файл отображается отдельным листом под своей папкой, а щелчок показывает в правой панели только выбранный файл.
- Кнопка «Только этот» оставляет для поиска один источник, а «Выбрать все» возвращает общий поиск.
- Источники можно выделять через Ctrl/Shift и через контекстное меню включать, исключать либо оставлять для поиска только всю выделенную группу.
- Поисковый запрос получает идентификаторы только включённых источников; при пустом выборе приложение предлагает выбрать хотя бы один источник.
- SQLite-поиск проверен тестом, в котором результаты приходят из двух выбранных источников, а третий источник исключается.
- При максимизации главное окно ограничивается рабочей областью текущего монитора, поэтому строка состояния остаётся над панелью задач Windows.
- Release-скрипт собирает приложение, запускает тесты, проверяет структуру поставки и создаёт ZIP с SHA-256.
- В корне portable-папки находятся только `VideoArchiveFinder.exe` и `app`.
- В поставку входят FFmpeg, FFprobe, x64 LibVLC с плагинами и обязательные лицензии.
- EXE из portable-папки имеет продуктовую версию `1.0.0-rc.2`, запускается, корректно максимизируется и штатно завершается с кодом 0.
- Реальный MXF по UNC распознан как MPEG-2 4:2:2, получил метаданные и миниатюру; пользователь подтвердил hover scrubbing.
- После завершения просмотра и закрытия приложения MXF освобождён и доступен для эксклюзивного чтения.

## Проверки

- Нативная оконная проверка после системной команды максимизации — границы окна `0,0–2560,1400` совпали с рабочей областью монитора `0,0–2560,1400`.
- `dotnet build .\VideoArchiveFinder.sln` — успешно, 0 предупреждений, 0 ошибок.
- `dotnet test .\VideoArchiveFinder.sln --no-build` — 255 тестов пройдено, 0 ошибок.
- `.\scripts\New-PortableRelease.ps1 -Version 1.0.0-rc.2` — сборка без предупреждений и ошибок, 245 тестов пройдено; ZIP и файл SHA-256 созданы, структура поставки проверена.
- Запуск EXE из распакованного ZIP — окно отвечает и штатно завершается с кодом 0.
- MXF `05_02_Реконструкция_битвы_01-29.mxf` — FFprobe и FFmpeg из RC успешно обработали файл; hover scrubbing подтверждён пользователем; исходник не изменён и не заблокирован.

## Ограничения и блокеры

- Текущий компьютер содержит .NET SDK; запуск на отдельном Windows x64 без установленного .NET и VLC ещё требует ручной проверки.
- Ручная приёмка на разных DPI и разрешениях экрана для этого RC ещё не выполнена.
- Выбор режима индексации, смешанное дерево поиска и групповые операции собраны и покрыты автоматическими тестами, но визуально в запущенном окне ещё не проверены.
- Ранее замеченные пользователем UI-глюки ещё не описаны воспроизводимыми сценариями.

## Следующий результат

Провести ручную приёмку трёх режимов нового источника и смешанного дерева результатов, затем включить изменение в следующий portable RC.

## Точка входа

- Release-скрипт: `scripts\New-PortableRelease.ps1`.
- Интерфейс выбора: `src\VideoArchiveFinder.Desktop\MainWindow.xaml`.
- Логика области поиска: `src\VideoArchiveFinder.Desktop\ViewModels\MainWindowViewModel.cs`.
- RC: `artifacts\releases\VideoArchiveFinder-1.0.0-rc.2-win-x64.zip`.
- Контрольная сумма: `artifacts\releases\VideoArchiveFinder-1.0.0-rc.2-win-x64.zip.sha256` (`6ce67be76ce968a3f775dbfa3ae5907628f1fa7b2c33df986d09a3819b9b203f`).
- Сценарии приёмки: `APP_BRIEF.md`.
